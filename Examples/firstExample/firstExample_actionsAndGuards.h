#ifndef FIRST_EXAMPLE_ACTIONS_AND_GUARDS_H
#define FIRST_EXAMPLE_ACTIONS_AND_GUARDS_H

    #include "firstExample_types.h"

    bool_t ready_query( event_p eventP, status_t status ) ;
    status_t start( event_p eventP, status_t status ) ;
    status_t stop( event_p eventP, status_t status ) ;

#endif